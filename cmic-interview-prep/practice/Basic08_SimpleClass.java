/*
 * Basic 8 — Simple Class
 *
 * Goal: define a class with fields, constructor, getters.
 *
 * Tasks:
 *   1. Define a class Person with two private fields:
 *        - String name
 *        - int age
 *   2. Add a constructor Person(String name, int age)
 *   3. Add getName() and getAge()
 *   4. Override toString() to return "Person[name, age]"
 *   5. In main, create a Person("Alice", 30) and print it
 */
public class Basic08_SimpleClass {

    // TODO: define class Person here (or as a separate inner static class)


    public static void main(String[] args) {
        // TODO: create a Person and print it


        System.out.println("--- expected ---");
        System.out.println("Person[Alice, 30]");
    }
}
