/*
 * Basic 3 — If / Else
 *
 * Goal: practice if / else if / else.
 *
 * Tasks:
 *   Given an int score, print:
 *     "A" if score >= 90
 *     "B" if score >= 80
 *     "C" if score >= 70
 *     "F" otherwise
 *
 * Try with score = 85 (should print "B").
 * Then change to 95, 72, 50 to verify each branch.
 */
public class Basic03_IfElse {
    public static void main(String[] args) {
        int score = 85;

        // TODO: write if / else if / else


        System.out.println("--- expected for 85 ---");
        System.out.println("B");
    }
}
